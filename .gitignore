# תלויות מותקנות — לא נכנסות לגיט
node_modules/

# תוצאת בנייה של Vite
dist/

# קבצי מערכת / עורך
.DS_Store
Thumbs.db
*.log
.vite/
.cursor/
.claude/
.env
.env.*
!.env.example

# מפתחות service account (Google וכו') — סודות, לא לגיט
secrets/

# דוחות שהסוכן bank-risk-expert מפיק (Markdown מקומי) — פלט, לא קוד
reports/
