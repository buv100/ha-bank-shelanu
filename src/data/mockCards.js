/**
 * ייצוא תאימות לאחור — מקור האמת ב־mockUsers (משתמש 1).
 * האפליקציה משתמשת ב־getCurrentCards.
 */

import { mockUsers } from './mockUsers.js';

/** כרטיסי משתמש הדמו הראשון */
export const mockCards = mockUsers[0].cards;
