# אפיון מוצר — הבנק שלנו

מסמך זה הוא **מקור האמת** למוצר. כל החלטה על פיצ'רים, מסכים, נתונים וארכיטקטורה מתחילה כאן.  
חוקי העבודה היומיומיים נמצאים ב־`rules.md`. תיעוד שינויים — ב־`change.md`.

---

## 1. חזון ומטרות

### 1.1 מה זה
**הבנק שלנו** הוא אתר בנק דמיוני ישראלי — דמו מקומי ללמידה ותרגול. המטרה ארוכת הטווח: להגיע לרמה שמרגישה כמו אתר בנק אמיתי (מסכים, זרימות, נתונים, אבטחה, עיצוב).

### 1.2 למה בונים
- ללמוד פיתוח frontend מאפס עד רמה גבוהה.
- להבין כל שכבה בקוד (HTML, CSS, JS, מבנה פרויקט, ואחר כך Backend / Firebase).
- לבנות מוצר אישי שמתפתח שלב־אחרי־שלב לפי בקשות מפורשות.

### 1.3 עקרון העבודה
הפרויקט **דינמי**: אין לוח זמנים קשיח לכל הפיצ'רים.  
מתחילים משלב 1 (התחברות דמה + עו״ש), ואז מוסיפים דברים **רק כשמבקשים במפורש** — עד שהרמה מספקת.

### 1.4 הצלחה לפי שלבים
| שלב | הצלחה נראית כך |
|-----|----------------|
| מסמכים | אפיון וחוקים ברורים; מסונכרנים עם הדמו |
| שלב 1 ✓ | התחברות דמה + עו״ש |
| דמו מתקדם (עכשיו) | כרטיסים, פרופיל, ריכוז, הלוואה, קניות/משכורת אוטומטיות, צ׳אט Groq |
| בהמשך | העברות, נחיתה, Firebase, אבטחה מתקדמת |
| יעד סופי | חוויית אתר בנק מלאה (במסגרת דמו/למידה — לא בנק חוקי אמיתי) |

---

## 2. משתמשים ותרחישים

### 2.1 קהל יעד
משתמש יחיד: **בעל הפרויקט** (שימוש אישי ללמידה). אין לקוחות אמיתיים ואין צוות בנק.

### 2.2 תפקידים
| תפקיד | סטטוס | תיאור |
|--------|--------|--------|
| לקוח פרטי | פעיל (דמו) | צופה ביתרה ובתנועות; בעתיד גם כרטיסים, העברות וכו' |
| מנהל בנק / תמיכה | לא בתוכנית כרגע | אפשר לשקול בעתיד אם יבוקש |
| עסק / חשבון חברה | לא בתוכנית כרגע | — |

### 2.3 כמה משתמשים בדמו
התחברות מול **Supabase Auth** (שם משתמש + סיסמה בלבד — בלי אימות מייל או SMS). אחרי כניסה נטענים פרופיל/עו״ש/כרטיסים מה־DB ל־localStorage.  
`mockUsers` נשאר כגיבוי אם אין `VITE_SUPABASE_*` ב־`.env`.  
עו״ש וכרטיסים נשמרים ב־`localStorage` לפי משתמש; **סימולטור קניות אוטומטי** מדמה תשלום בכרטיס (חיוב→עו״ש, אשראי→חוב כרטיס) דרך `simulateCardPurchase` — כתיבה חזרה ל־DB בהמשך.  
**משכורת אוטומטית:** בכל **10 לחודש** נכנסת משכורת (`monthlySalary`) לכל חשבונות הדמו, עם השלמה (catch-up) אם הדף היה סגור.  
תפקיד `admin` ומסך ניהול — **לא בשלב זה** (מוכן להרחבה בהמשך).

### 2.4 תרחישי ליבה (דמו נוכחי)
1. המשתמש פותח את האתר → רואה דף כניסה/הרשמה (שם משתמש + סיסמה).
2. אחרי הרשמה/התחברות → אם חשבון חדש, דף פתיחת חשבון (פרטי קשר, משכורת, כרטיסים, הוצאות, השקעות). מה שלא מולא נוצר אוטומטית (סניף, יתרה, מספרי כרטיס).
3. סשן Auth + hydrate מה־DB → אזור מחובר (ריכוז / עו״ש / כרטיסים / פרופיל).
4. רואה יתרה, תנועות וכרטיסים; קניות אוטומטיות, הוצאות קבועות ומשכורת ב־10 לחודש מעדכנות את הנתונים.
5. צ׳אט «הבנקאי» זמין באזור המחובר (מצב רגיל / רגיש; שיחה נשמרת בסגירה; מחיקה בכפתור ייעודי).
6. יציאה מנקה את ה־session.

---

## 3. היקף המוצר

### 3.1 מוצרים בנקאיים
| מוצר | שלב נוכחי | הערות |
|------|-----------|--------|
| עובר ושב (עו״ש) | כן | חשבון דמו + תנועות + משכורת חודשית ב־10 לחודש |
| כרטיסי אשראי / חיוב | כן | חיוב יורד מעו״ש; אשראי עם מסגרת/חוב/תנועות; קניות אוטומטיות; הצגת פרטים מלאים עם אישור |
| ריכוז יתרות | כן | עו״ש נטו מול חוב אשראי (+ מקום להשקעות בעתיד) |
| הלוואה (דמו) | כן | דף בקשה + כרטיס פרסומת ליד היתרה |
| השקעות | בהמשך | אחרי אשראי |
| חסכון / פיקדון / מט״ח | רעיונות לעתיד | רק אם יבוקשו |

### 3.2 יכולות קיימות בדמו (מעבר לשלב 1)
- פרופיל והגדרות (הסתרת יתרה/הלוואה, התראות, מצב כהה)
- צ׳אט AI («הבנקאי») — שיחה פתוחה דרך Groq + פעולות מקומיות (ניווט, כרטיס, העדפות)
- שמירת היסטוריית צ׳אט בסשן; בחירת מצב רגיל/רגיש; פרטי כרטיס נפתחים בדף הכרטיסים (לא בתוך הצ׳אט)
- מדיניות פרטיות / תנאי שימוש / נגישות (דפי מידע)

### 3.3 יכולות עתידיות (רשימת כוונות)
לא מחויבות לסדר קשיח; יתווספו לפי בקשה:
- דף נחיתה שיווקי לפני התחברות
- העברת כסף (בין חשבונות / למוטב)
- פירוט תנועה בודדת, סינון וחיפוש
- תשלום חשבונות, התראות אמיתיות, תקציב
- Firebase (אימות + שמירת נתונים)
- אבטחה מתקדמת (2FA, יומן פעולות, הגבלות העברה)

### 3.4 מה לא בונים כרגע
- כתיבה מלאה של עו״ש/כרטיסים/השקעות חזרה ל־Supabase (קריאה בכניסה כבר קיימת)
- העברות כסף אמיתיות
- אפליקציית מובייל נפרדת (רק אתר רספונסיבי)
- מפתחות API בדפדפן — המפתח נשאר בשרת Vite / Worker (`.env`, לא `VITE_`)

---

## 4. שלב 1 — MVP דמו

### 4.1 מטרת השלב
דמו מקומי קצר: התחברות למראה → מסך עו״ש עם יתרה ותנועות. בסיס נקי להמשך.

### 4.2 מסכים
1. **דף התחברות (`login`)**
   - שדות: אימייל, סיסמה (ויזואליים בלבד בשלב זה).
   - כפתור "היכנס": **תמיד מצליח** — גם אם השדות ריקים; אין בדיקת תקינות מול DB.
   - מיתוג: שם הבנק + לוגו + צבעי לבן/טורקיז.
2. **דף עובר ושב (`account` / checking)**
   - הצגת **יתרה נוכחית**: 15,000 ₪ (ניתנת לשינוי בקוד / בנתוני דמה).
   - רשימת **10 תנועות דמה** (הכנסות והוצאות) — בלי סינון/חיפוש בשלב זה.
   - מותאם למובייל ולדסקטופ.

### 4.3 זרימה
```
דף התחברות → לחיצה על "היכנס" → דף עובר ושב
                              → יתרה 15,000 ₪
                              → 10 תנועות דמה
```

### 4.4 נתוני דמה (שלב 1)
- **מטבע:** שקל חדש (₪) בלבד.
- **יתרה:** 15,000.
- **תנועות:** 10 רשומות מומצאות בעברית (שמות ריאליסטיים: משכורת, סופר, דלק, וכו').
- מקור הנתונים: קובץ/מודול mock בקוד — **לא** localStorage ו**לא** שרת בשלב 1.
- היתרה והתנועות **ניתנים לשינוי** בקלות בקובץ הנתונים.

### 4.5 התנהגות התחברות (דמו)
- אין הרשמה.
- אין שחזור סיסמה.
- אין שמירת session אמיתית מול שרת (אפשר סימון מקומי פשוט לניתוב בין דפים אם נדרש להרצה).
- רענון הדף: התנהגות תוגדר במימוש; העיקר בשלב 1 הוא שהמעבר Login → Account עובד בלחיצה.

### 4.6 בתוך ההיקף / מחוץ להיקף (שלב 1)

**בתוך:**
- UI התחברות + UI עו״ש
- לוגו בסיסי
- 10 תנועות + יתרה
- RTL, עברית, רספונסיבי, עיצוב לבן+טורקיז

**מחוץ:**
- ולידציה אמיתית, שגיאות התחברות, OTP
- עריכת/מחיקת תנועות מהממשק
- גרפים מורכבים, סינון, ייצוא
- Firebase / Backend

### 4.7 קריטריוני סיום שלב 1
- [x] הפרויקט רץ מקומית עם Vite.
- [x] דף התחברות מוצג ומעוצב.
- [x] לחיצה על "היכנס" מעבירה לעו״ש בלי צורך בפרטים.
- [x] בעו״ש מוצגים יתרה 15,000 ₪ ו־10 תנועות דמה.
- [x] האתר קריא במובייל ובדסקטופ.
- [x] הקוד מחולק לקבצים ייעודיים עם הערות בעברית לפי `rules.md`.
- [x] כל שינוי מתועד ב־`change.md`.

---

## 5. מפת מסכים ויכולות

### 5.1 קיים בדמו
| מסך / יכולת | תיאור קצר |
|-------------|-----------|
| התחברות | שם משתמש + סיסמה; הרשמה וכניסה מיד (בלי אימות מייל/SMS) |
| פתיחת חשבון | הזנת פרטים ראשוניים; השלמה אוטומטית ליתרה/סניף/כרטיסים |
| ריכוז | יתרת עו״ש, חוב אשראי, נטו |
| עו״ש | יתרה, תנועות, משכורת ב־10 לחודש, קניות אוטומטיות |
| כרטיסים | בחירה, מסגרת/חוב, הצגה מלאה עם אישור וטיימר |
| פרופיל | פרטים + העדפות + יציאה |
| הלוואה | דף בקשה (לא בתפריט; מפרסומת בכותרת) |
| צ׳אט AI | FAB + מצבי רגיל/רגיש + Groq + פעולות מקומיות |

### 5.2 עדיפויות בהמשך (לפי בקשה)
| עדיפות | מסך / יכולת | תיאור קצר |
|--------|-------------|-----------|
| 1 | דף נחיתה | שיווק לפני התחברות |
| 2 | העברת כסף | טופס העברה (בדמו ואז לוגיקה עשירה יותר) |
| 3 | סינון / פירוט תנועה | חיפוש ופירוט בעו״ש |
| 4 | Firebase / אבטחה | Auth אמיתי, 2FA למראה ואז אמיתי |

---

## 6. עיצוב ומותג

### 6.1 זהות
- **שם:** הבנק שלנו
- **שפה:** עברית מלאה, כיוון RTL
- **תחושה:** ידידותי־צעיר וגם יוקרתי — נקי, בהיר, לא "דשבורד כבד"
- **לוגו:** נבנה בפרויקט (SVG או טקסט מעוצב) — placeholder איכותי בהתחלה, ניתן לשדרוג

### 6.2 צבעים
- בסיס: **לבן** ורקעים בהירים עם עומק עדין (גרדיאנט/טקסטורה קלה — לא צבע שטוח בלבד)
- דגש (accent): **טורקיז**
- טקסט: כהה לקריאות גבוהה על רקע בהיר
- מצבי תנועה: הכנסה / הוצאה — צבעי משנה ברורים (לא חייבים טורקיז לשניהם)

צבעים מדויקים (hex) יוגדרו במשתני CSS במימוש הראשון ויירשמו ב־`change.md` / בעדכון קצר לאפיון אם ישתנו.

### 6.3 טיפוגרפיה
- פונטים מכוונים (לא ברירת־מחדל מערכתית גנרית בלבד).
- היררכיה ברורה: כותרת בנק / יתרה / רשימת תנועות.
- בלי עומס טקסטual במסך הראשון של כל שלב.

### 6.4 עקרונות UI
- מסך פשוט = מטרה אחת ברורה.
- שלב 1: לא דשבורד עם עשרות ווידג'טים.
- רספונסיבי: דסקטופ ומובייל חובה.
- 2–3 אנימציות עדינות ומכוונות (כניסה לדף, הופעת רשימה, מעבר כפתור) — לא רעש ויזואלי.
- להימנע ממראה "AI גנרי" (סגול־על־לבן, cream+terracotta, וכו').

### 6.5 נגישות בסיסית (כוונה)
- ניגודיות טקסט מספקת, תוויות לשדות, פוקוס מקלדת סביר — ישודרג עם הזמן.

---

## 7. ארכיטקטורה

### 7.1 סטק נוכחי
- **Vite + Vanilla JavaScript**
- HTML + CSS מודרני
- בלי React / Vue / Angular (אלא אם יאושר במפורש בעתיד)
- הרצה מקומית (`npm install` + `npm run dev`)
- צ׳אט AI: פרוקסי Vite מקומי ל־**Groq** (`server/groqChat.js` + `GROQ_API_KEY` ב־`.env`); אופציונלי Cloudflare Worker לאותו תפקיד בפריסה

### 7.2 אבולוציית שכבת נתונים
```
שלב 1:     קבצי mock בקוד (JS modules)
עכשיו:     mockUsers + localStorage (עו״ש/כרטיסים) + sessionStorage (session/צ׳אט)
אופציונלי: Groq API דרך פרוקסי שרת (לא בדפדפן)
סכמה:      Postgres ב־Supabase (db/schema.sql)
עכשיו:     Auth + קריאה מה־DB בכניסה; כתיבות יומיומיות עדיין ב־localStorage
אחר כך:    כתיבה מלאה של עו״ש/כרטיסים/השקעות חזרה ל־DB
```

### 7.3 מבנה תיקיות (עקרון נוכחי)
```
specs.md / rules.md / change.md
package.json / vite.config.js / .env.example
*.html                  # login, overview, account, cards, profile, loan, legal…
db/                     # סכמת Postgres + seed + README (Supabase)
server/                 # פרוקסי צ׳אט מקומי (Groq) — לא נארז לדפדפן
worker/                 # Cloudflare Worker אופציונלי לצ׳אט
src/
  *Main.js              # נקודות כניסה לדפים
  styles/ pages/ data/ ui/ utils/ services/
```

### 7.4 עקרון הפרדת אחריות
- כל קובץ אחראי על נושא אחד.
- UI לא מחזיק לוגיקת נתונים כבדה.
- נתוני דמה לא מעורבבים בתוך קבצי עיצוב.
- פונקציות קצרות; בלי "קובץ ענק אחד".
- סודות API רק בשרת (`.env` / Worker secret) — לא ב־`VITE_*`.

### 7.5 ניתוב
- לפני התחברות: `index.html`.
- אחרי התחברות: דפים עם **מעטפת אפליקציה** משותפת (כותרת עליונה + תפריט תחתון):
  - `overview.html` — ריכוז יתרות
  - `account.html` — עו״ש
  - `cards.html` — כרטיסים
  - `profile.html` — פרופיל
- `loan.html` — בקשת הלוואה; **לא** בתפריט; נפתח מפרסומת "לפרטים".
- צ׳אט AI מרחף בכל דפי האזור המחובר.

---

## 8. מודל נתונים

### 8.1 ישויות — שלב 1

**User (דמו / למראה)**
| שדה | סוג | הערות |
|-----|-----|--------|
| id | string | מזהה דמה |
| email | string | מוצג בטופס; לא מאומת |
| displayName | string | אופציונלי לתצוגה בעו״ש |

**Account (עו״ש)**
| שדה | סוג | הערות |
|-----|-----|--------|
| id | string | מזהה חשבון |
| type | `"checking"` | כרגע רק עו״ש |
| currency | `"ILS"` | שקל |
| balance | number | 15000 בהתחלה |

**Transaction**
| שדה | סוג | הערות |
|-----|-----|--------|
| id | string | ייחודי |
| date | string (ISO או YYYY-MM-DD) | תאריך התנועה |
| description | string | תיאור בעברית |
| amount | number | חיובי = הכנסה, שלילי = הוצאה (או שדה type נפרד) |
| category | string | אופציונלי לפירוט עתידי |

### 8.2 ישויות קיימות / מורחבות בדמו
- **DemoUser** — `id`, `profile`, `account`, `monthlySalary`, `transactions`, `cards`
- **Card** — מספר ממוסך/מלא (דמו), תוקף, CVV, סוג (חיוב/אשראי), מסגרת/חוב ב־runtime
- **Profile prefs** — הסתרת יתרה/הלוואה, התראות (sessionStorage)

### 8.3 סכמת Postgres (מוכנה, לא מחוברת ל־UI)
מוגדרת ב־`db/schema.sql`. עקרונות: UUID, `numeric` לכסף, RLS לפי `auth.uid()`, בלי סיסמאות/PAN/CVV בטבלאות שלנו.

| טבלה | תפקיד |
|------|--------|
| `profiles` | לקוח (`id` = `auth.users.id`) + `username` |
| `branches` | סניפים |
| `accounts` / `transactions` | עו״ש ותנועות (`source` כולל `simulator`) |
| `cards` / `card_transactions` | כרטיסים (חסימה, חוב; בלי מספר מלא) |
| `market_instruments` / `holdings` / `watchlist` | שוק + תיק (`instrument_id` חובה) |
| `user_prefs` / `loan_applications` | העדפות ובקשת הלוואה |
| `purchase_templates` / `simulator_state` | סימולטור קניות + `last_run_at` |
| `audit_log` | יומן פעולות (insert/select בלבד) |

### 8.4 ישויות עתידיות (שלד)
- **Transfer** — מקור, יעד, סכום, תאריך, סטטוס

### 8.5 דוגמת JSON לדמו (עקרון)
```json
{
  "account": {
    "id": "acc-1",
    "type": "checking",
    "currency": "ILS",
    "balance": 15000
  },
  "transactions": [
    {
      "id": "tx-1",
      "date": "2026-07-28",
      "description": "משכורת חודשית",
      "amount": 12000,
      "category": "הכנסה"
    },
    {
      "id": "tx-2",
      "date": "2026-07-27",
      "description": "סופרמרקט",
      "amount": -245.9,
      "category": "קניות"
    }
  ]
}
```
(ברשימה המלאה יהיו 10 תנועות בקובץ ה־mock.)

(ברשימה המלאה — תנועות לפי משתמש ב־`mockUsers`; משכורת חוזרת ב־10 לחודש דרך `salaryDeposit`.)

### 8.6 כללי תצוגה
- סכומים בפורמט עברי / מקומי עם ₪.
- תאריכים קריאים בעברית או בפורמט יום/חודש/שנה עקבי.
- הכנסה והוצאה מובחנות ויזואלית ברשימה.

---

## 9. צ׳אט AI («הבנקאי»)

### 9.1 מטרה
עוזר שיחה באזור המחובר — שיחה טבעית + פעולות דמו (ניווט, העדפות, פתיחת פרטי כרטיס בדף הכרטיסים).

### 9.2 מצבים
| מצב | מה מותר |
|-----|---------|
| רגיל | שיחה כללית ומעבר בין דפים / הגדרות — בלי יתרה או פרטי כרטיס מלאים |
| רגיש | אחרי אישור חד־פעמי בכניסה — גם יתרה ובחירת כרטיס להצגה בדף הכרטיסים |

במצב רגיל, בקשה רגישה מציעה מעבר למצב רגיש (עם אישור).

### 9.3 חיבור AI
- ספק נוכחי: **Groq** (מודל ברירת מחדל `openai/gpt-oss-120b`, `reasoning_effort: low`).
- מפתח ב־`.env` כ־`GROQ_API_KEY` — נקרא רק בשרת Vite (`/api/chat`) או ב־Worker.
- בלי מפתח / כשל רשת — נפילה לתשובות דמה מקומיות + פעולות מזוהות.

### 9.4 פרטיות בדמו
- פרטי כרטיס מלאים **לא** נשלחים כטקסט בצ׳אט — רק בחירה ואז פתיחת מודל/דף כרטיסים.
- היסטוריית שיחה נשמרת בסשן; סגירת הפאנל לא מוחקת; מחיקה רק בכפתור הפח.

---

## 10. אבטחה

### 10.1 דמו נוכחי
- אימות מול Supabase Auth; סיסמה רק ב־`auth.users`. בדפדפן רק מפתח publishable.
- מפתחות API (Groq) לא נכנסים לקוד הציבורי ולא ל־`VITE_*`.
- הסכמות צ׳אט: שימוש ב־AI + אישור מצב רגיש לפני נתונים רגישים.

### 10.2 סכמת DB
- Postgres ב־Supabase: RLS לפי משתמש מחובר.
- אין מספר כרטיס מלא ואין CVV בטבלאות.
- יומן `audit_log` מוגדר בסכמה.

### 10.3 בהמשך
- כתיבה מלאה של stores חזרה ל־DB.
- 2FA — למראה ואז אמיתי לפי החלטה.

---

## 11. איכות ולמידה

הפרויקט הוא כלי לימוד. לכן:
- כל קוד חייב להיות **מובן שורה־שורה** עם הערות בעברית (פירוט ב־`rules.md`).
- שמות משתנים ופונקציות באנגלית ברורה (`getBalance`, `renderTransactions`).
- ממשק המשתמש בעברית.
- פונקציות קצרות; קבצים ייעודיים; בלי ערבוב אחריות.
- כשנשאלת שאלה על הקוד — אפשר להצביע לקובץ ולשורה ולהסביר במהירות.

---

## 12. Roadmap גמיש

```
[מסמכים] specs + rules + change
    ↓
[שלב 1 ✓] Login דמה + עו״ש
    ↓
[בוצע בדמו] כרטיסים · פרופיל · ריכוז · הלוואה · קניות אוטומטיות · משכורת חודשית · צ׳אט Groq
    ↓
[לפי בקשה] נחיתה · העברות · סינון · Firebase · אבטחה מתקדמת
    ↓
[יעד] חוויית אתר בנק ברמה גבוהה (דמו מתקדם)
```

אין תאריכי יעד קשיחים. כל שלב חדש מתחיל בבקשה מפורשת, מתעדכן ב־`specs.md` אם צריך, ומתועד ב־`change.md`.

---

## 13. פלטפורמה ופריסה

| נושא | החלטה |
|------|--------|
| סביבה נוכחית | מקומי (+ פריסת GitHub Pages אופציונלית) |
| מובייל | אתר רספונסיבי — לא אפליקציה נפרדת |
| צ׳אט AI | Groq דרך פרוקסי מקומי / Worker |
| דפדפנים | דפדפנים מודרניים עדכניים |

---

## 14. מסמכים נלווים

| קובץ | תפקיד |
|------|--------|
| `specs.md` | אפיון מוצר — מקור האמת |
| `rules.md` | חוקי עבודה וקוד — נגזרים מהאפיון |
| `change.md` | יומן שינויים — חובה אחרי כל שינוי |

כשיש סתירה בין הרגל ישן לבין המסמכים: **`specs.md` למוצר**, **`rules.md` לאופן העבודה בקוד**. אם סתירה פנימית — מעדכנים את המסמכים קודם (ומתעדים ב־`change.md`).

---

## 15. מילון מונחים קצר

| מונח | משמעות בפרויקט |
|------|----------------|
| דמו | אתר מקומי בלי Backend אמיתי מלא |
| עו״ש | חשבון עובר ושב — יתרה + תנועות |
| Mock | נתונים מומצאים בקוד |
| Accent | צבע הדגש (טורקיז) |
| RTL | כיוון ממשק מימין לשמאל |
| מצב רגיש | מצב צ׳אט שמאפשר יתרה/פרטי כרטיס אחרי אישור |
| Groq | ספק ה־AI הנוכחי לצ׳אט |

---

*האפיון מתעדכן לפי התקדמות הדמו — שלב 1 הושלם; הסעיפים למעלה משקפים את המצב הנוכחי.*
